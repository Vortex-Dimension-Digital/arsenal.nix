{ greenbone-feed-sync }:

greenbone-feed-sync
// {
  meta = greenbone-feed-sync.meta // {
    description = "Compatibility command for synchronizing Greenbone CERT data";
    mainProgram = "greenbone-certdata-sync";
  };
}
