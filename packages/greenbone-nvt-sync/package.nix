{ greenbone-feed-sync }:

greenbone-feed-sync
// {
  meta = greenbone-feed-sync.meta // {
    description = "Compatibility command for synchronizing Greenbone vulnerability tests";
    mainProgram = "greenbone-nvt-sync";
  };
}
