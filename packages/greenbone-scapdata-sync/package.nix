{ greenbone-feed-sync }:

greenbone-feed-sync
// {
  meta = greenbone-feed-sync.meta // {
    description = "Compatibility command for synchronizing Greenbone SCAP data";
    mainProgram = "greenbone-scapdata-sync";
  };
}
